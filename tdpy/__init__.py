"""TouchDesigner-Python - a project's structure kept in text, not in the .toe.

The .toe holds only an Execute DAT that reaches this package (see
`DAT/StartupExec.py`); everything the project actually is lives here.
"""
